# API 요청 기준이 되는 origin (server action 에서 사용)
# 로컬 개발 시에는 standalone mock 서버(pnpm mock)를 바라보게 설정합니다.
NEXT_PUBLIC_BASE_URL=http://localhost:9090

# MSW 브라우저 모킹 활성화 여부 ("enabled" 일 때만 동작)
NEXT_PUBLIC_API_MOCKING=enabled
